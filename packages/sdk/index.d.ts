/**
 * Aether SDK - TypeScript definitions for Diablo II scripting
 *
 * This package provides type definitions for the `diablo:` module scheme,
 * which allows importing SDK modules using URL-style imports similar to Node.js's `node:` scheme.
 *
 * @example
 * ```typescript
 * import { createBot, game } from "diablo:game"
 * import { test, assert } from "diablo:test"
 * import { log, getArea } from "diablo:native"
 * ```
 */

/// <reference path="./diablo.d.ts" />
