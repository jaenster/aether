// Test script — loaded via daemon
const area = getArea();
const x = getUnitX();
const y = getUnitY();
const hp = getUnitHP();
const maxHp = getUnitMaxHP();

"script loaded: area=" + area + " pos=" + x + "," + y + " hp=" + hp + "/" + maxHp;
